

```
┌──────────────────────────────────────────────────────────────┐
│ 1. External Attacker (Your Kali Box)                         │
└──────────────────────────────────────────────────────────────┘
                │
                │  Web enumeration → PWM endpoint
                ▼
┌──────────────────────────────────────────────────────────────┐
│ 2. PWM Web App (pwm.fries.htb)                               │
│    • LDAP injection                                          │
│    • Extract svc_infra credentials                           │
└──────────────────────────────────────────────────────────────┘
                │
                │  LDAP bind with svc_infra
                ▼
┌──────────────────────────────────────────────────────────────┐
│ 3. Active Directory (FRIES.HTB)                              │
│    • Read secrets stored in LDAP                             │
│    • Retrieve PostgreSQL credentials                         │
└──────────────────────────────────────────────────────────────┘
                │
                │  Connect to DB (internal‑only)
                ▼
┌──────────────────────────────────────────────────────────────┐
│ 4. PostgreSQL Container (172.18.0.x)                         │
│    • SQL → RCE → reverse shell                               │
│    • First foothold inside Docker network                    │
└──────────────────────────────────────────────────────────────┘
                │
                │  Lateral enumeration inside 172.18.0.0/16
                ▼
┌──────────────────────────────────────────────────────────────┐
│ 5. Docker Internal Services                                  │
│    • Gitea → secrets                                         │
│    • PgAdmin → DB mgmt                                       │
│    • fries-web → configs                                     │
│    • pwm → LDAP creds                                        │
└──────────────────────────────────────────────────────────────┘
                │
                │  Loot host‑level credentials from configs
                ▼
┌──────────────────────────────────────────────────────────────┐
│ 6. SSH Access to Host (192.168.100.2)                        │
│    • User: svc                                               │
│    • Now outside Docker, on the real OS                      │
└──────────────────────────────────────────────────────────────┘
                │
                │  Enumer![[Diagram.svg|912x973]]ate host → find NFS export
                ▼
┌──────────────────────────────────────────────────────────────┐
│ 7. NFS Export (/srv/web.fries.htb)                           │
│    • no_root_squash                                          │
│    • writable by attacker                                    │
└──────────────────────────────────────────────────────────────┘
                │
                │  Write SSH key as root via NFS
                ▼
┌──────────────────────────────────────────────────────────────┐
│ 8. Root on Host (192.168.100.2)                              │
│    • Full control of Docker Engine                           │
│    • Full filesystem access                                  │
└──────────────────────────────────────────────────────────────┘
                │
                │  Dump secrets from host + containers
                ▼
┌──────────────────────────────────────────────────────────────┐
│ 9. Domain Credentials (Administrator)                        │
│    • Extracted from host/containers                          │
└──────────────────────────────────────────────────────────────┘
                │
                │  WinRM / SMB / LDAP / Kerberos
                ▼
┌──────────────────────────────────────────────────────────────┐
│ 10. Domain Admin (FRIES.HTB)                                 │
│     • Full domain compromise                                 │
└──────────────────────────────────────────────────────────────┘
```

![[Diagram.svg|912x973]]



# 🔥 **Pivot Logic Explained (Why Each Step Works)**

### **1 → 2: Web → PWM**

PWM is the only externally reachable service that talks to LDAP. That makes it the first trust boundary.

### **2 → 3: PWM → AD**

LDAP injection gives you a real domain user. This is your first identity pivot.

### **3 → 4: AD → PostgreSQL**

AD stores DB credentials → you pivot into the internal DB.

### **4 → 5: PostgreSQL → Docker Network**

SQL RCE gives you a shell inside a container. This is your first _network_ pivot.

### **5 → 6: Docker → Host**

Container configs leak host SSH creds. This is your escape from the sandbox.

### **6 → 7: Host → NFS**

Host exports a writable NFS share. This is your privilege escalation vector.

### **7 → 8: NFS → Root**

Write your SSH key as root → instant host takeover.

### **8 → 9: Root → Domain Secrets**

Root can read everything, including AD‑related secrets.

### **9 → 10: Domain → DA**

Final identity pivot → full domain compromise.