

                          ┌───────────────────────────────┐
                          │ 10.10.11.96 (HTB Public Host) │
                          │  • Windows DC01.fries.htb     │
                          │  • Linux host (Docker engine) │
                          └──────────────┬────────────────┘
                                        │
                                         │
                         Public Services │
                                         ▼
                ┌──────────────────────────────────────────────┐
                │ Windows Domain Controller (DC01.fries.htb)   │
                │  • LDAP, Kerberos, SMB, DNS, RPC             │
                └──────────────────────────────────────────────┘

                                         │
                                         │
                                         ▼
                ┌──────────────────────────────────────────────┐
                │ Linux Host (Ubuntu 22.04) – 192.168.100.2    │
                │  • SSH (svc)                                 │
                │  • NFS export /srv/web.fries.htb             │
                │  • Docker Engine (TLS on 2376)               │
                └──────────────────────────────────────────────┘
                                         │
                                         │ Docker bridge
                                         ▼
                ┌──────────────────────────────────────────────┐
                │ Docker Network 172.18.0.0/16                  │
                │                                               │
                │ 172.18.0.1 → Docker Gateway                   │
                │ 172.18.0.3 → PostgreSQL (ps_db)               │
                │ 172.18.0.X → Gitea (code.fries.htb)           │
                │ 172.18.0.X → PgAdmin (db-mgmt05.fries.htb)    │
                │ 172.18.0.X → PWM (pwm.fries.htb)              │
                │ 172.18.0.X → Web App                          │
                └──────────────────────────────────────────────┘

---


┌──────────────────────────────────────────────────────────────────────────────┐
│                           EXTERNAL ATTACKER (You)                            │
└──────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ HTTP/HTTPS enumeration
                                      ▼
                         fries.htb  /  pwm.fries.htb  /  code.fries.htb
                                      │
                                      │ Credential leak via PWM → LDAP Injection
                                      ▼
                     ┌──────────────────────────────────────────┐
                     │  svc_infra@FRIES.HTB (AD User Creds)     │
                     └──────────────────────────────────────────┘
                                      │
                                      │ LDAP bind → read secrets
                                      ▼
                     ┌──────────────────────────────────────────┐
                     │  PostgreSQL creds (from PWM)             │
                     └──────────────────────────────────────────┘
                                      │
                                      │ Connect to internal DB
                                      ▼
                     ┌──────────────────────────────────────────┐
                     │  PostgreSQL Container (172.18.0.x)       │
                     └──────────────────────────────────────────┘
                                      │
                                      │ RCE via SQL → reverse shell
                                      ▼
                     ┌──────────────────────────────────────────┐
                     │  foothold inside Docker network          │
                     │  (172.18.0.0/16)                         │
                     └──────────────────────────────────────────┘
                                      │
                                      │ Enumerate containers
                                      ▼
        ┌────────────────────────────────────────────────────────────────────────┐
        │  Gitea → PgAdmin → WebApp → PWM containers (all reachable internally) │
        └────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ Credential reuse / config loot
                                      ▼
                     ┌──────────────────────────────────────────┐
                     │  svc@HOST (SSH credentials found)        │
                     └──────────────────────────────────────────┘
                                      │
                                      │ SSH into host (Ubuntu 22.04)
                                      ▼
                     ┌──────────────────────────────────────────┐
                     │  Host Machine: 192.168.100.2             │
                     │  • Docker Engine                         │
                     │  • NFS Export                            │
                     └──────────────────────────────────────────┘
                                      │
                                      │ Enumerate NFS exports
                                      ▼
                     ┌──────────────────────────────────────────┐
                     │  /srv/web.fries.htb *(rw,no_root_squash) │
                     └──────────────────────────────────────────┘
                                      │
                                      │ Mount NFS → write SSH key
                                      ▼
                     ┌──────────────────────────────────────────┐
                     │  Root on Host (via NFS privilege abuse)  │
                     └──────────────────────────────────────────┘
                                      │
                                      │ Access Docker Engine as root
                                      ▼
                     ┌──────────────────────────────────────────┐
                     │  Full control of all containers          │
                     └──────────────────────────────────────────┘
                                      │
                                      │ Dump secrets → AD creds
                                      ▼
                     ┌──────────────────────────────────────────┐
                     │  Administrator@FRIES.HTB (Domain Admin)  │
                     └──────────────────────────────────────────┘
                                      │
                                      │ WinRM / SMB / LDAP / Kerberos
                                      ▼
                     ┌──────────────────────────────────────────┐
                     │  Full Domain Compromise (DA)             │
                     └──────────────────────────────────────────┘


# 🔥 **Relationship‑Driven Summary (BloodHound Style)**

### **1. Web → LDAP**

- PWM leaks LDAP bind capability
    
- LDAP injection → extract svc_infra credentials
    

### **2. LDAP → PostgreSQL**

- LDAP‑stored secrets include DB credentials
    
- DB reachable only from Docker network
    

### **3. PostgreSQL → Docker Foothold**

- SQL RCE → reverse shell inside container
    
- You now live in 172.18.0.0/16
    

### **4. Docker → Host**

- Looted configs reveal SSH creds for `svc`
    
- SSH into host (192.168.100.2)
    

### **5. Host → NFS**

- Host exports `/srv/web.fries.htb` with write access
    
- No root squash → privilege escalation
    

### **6. NFS → Root**

- Write authorized_keys as root
    
- SSH in as root
    

### **7. Root → Docker Engine**

- Full control of all containers
    
- Extract secrets, tokens, AD creds
    

### **8. Secrets → Domain Admin**

- Recovered Administrator credentials
    
- WinRM → full domain compromise



---
